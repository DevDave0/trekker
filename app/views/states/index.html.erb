<h1>States</h1>

<% if flash[:message] %>
    <div class='error'><%= flash[:message] %></div>
<% end %>

<ul> 
    <% @states.each do |state| %>
        <li><%= link_to state.name, state_path(state.id) %></li>
    <% end %>
</ul>