
<div class='login'>
<h1 class='title is-1'>All trails</h1>
<%
=begin%>
 <%= select("trail", "name", Trail.all.collect(&:name).sort) %> 
<%
=end%>

<ul>
    <% @trails.each do |trail| %>
        <li><%= link_to trail.name, trail_path(trail.id) %></li>
    <% end %>
</ul>
</div>